apiVersion: v1
kind: Secret
metadata:
  name: {{ include "integration-service.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels: {{- include "integration-service.labels" . | nindent 4 }}
type: Opaque
data:
  DB_PASSWORD: {{ .Values.secrets.DB_PASSWORD | b64enc | quote }}
